import Vue from 'vue'

import { Meta, Props, Story, Preview } from '@storybook/addon-docs/blocks';
import SelectBox, { SelectBoxItem } from './SelectBox.vue';

<Meta title="SelectBox" component={SelectBox} />

# SelectBox

<Preview>
  <Story name="default">
    {{
      components: { SelectBox },
      data() {
        return {
          value: '',
        }
      },
      template: `
        <div>
          <SelectBox :options="options" v-model="value" placeholder="placeholder" />
          <p>{{value}}</p>
        </div>
      `,
      props: {
        options: {
          default: () => [
            {value: 'hoge key', label: 'hoge label'},
            {value: 'fuga key', label: 'fuga label'},
          ],
        },
      },
    }}
  </Story>
</Preview>


```typescript
import { SelectBox } from 'lapras-frontend'
```

```html
<SelectBox v-model="value" :options="[
  {value: 'hoge key', label: 'hoge label'},
  {value: 'fuga key', label: 'fuga label'}
]" />
```

<Props of={SelectBox} />
