import Vue from 'vue'

import { Meta, Props, Story, Preview } from '@storybook/addon-docs/blocks';
import EnhancedIconButton from './EnhancedIconButton.vue';
import Icon from '@/components/Icon/Icon.vue';

<Meta title="EnhancedIconButton" component={EnhancedIconButton} />

# EnhancedIconButton

<Preview>
  <Story name="default">
    {{
      data() {
        return {
          count: 0,
        }
      },
      components: { EnhancedIconButton,Icon },
      template: `
        <div style="width: 40px;">
          <EnhancedIconButton @click="count++">
            <Icon name="plus" />
          </EnhancedIconButton>
          <p>{{count}}</p>
        </div>`,
    }}
  </Story>
</Preview>

```typescript
import { EnhancedIconButton } from 'lapras-frontend'
```

```html
<EnhancedIconButton @click="action">
  <Icon name="stylish-plus" />
</EnhancedIconButton>
```

<Props of={EnhancedIconButton} />
