import Vue from 'vue'

import { Meta, Props, Story, Preview } from '@storybook/addon-docs';
import Icon from './Icon.vue';
import iconMap from './iconMap';

<Meta title="Icon" component={Icon} />

# Icon

<Preview>
  <Story name="default">
    {{
      components: { Icon },
      template: '<Icon name="thumbs-up" />',
    }}
  </Story>
</Preview>

```typescript
import { Icon } from 'lapras-frontend'
```

```html
<Icon name="thumbs-up" />
```

<Props of={Icon} />

## icon list

<Preview>
  <Story name="icon list">
    {{
        components: { Icon },
        template: `<table width="100%" style="color: #2C2C2C;">
          <tr v-for="name in Object.keys(iconMap)" :key="name">
            <td style="font-size: 38px; border-bottom: 1px solid #efefef; width: 80px;">
              <Icon :name="name" />
            </td>
            <td style="border-bottom: 1px solid #efefef;">{{name}}</td>
          </tr>
        </table>`,
        props: {
          iconMap: {
            default: iconMap,
          },
        },
    }}
  </Story>
</Preview>
