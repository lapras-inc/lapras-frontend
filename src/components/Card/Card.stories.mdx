import Vue from 'vue'
import VueCompositionApi from 'vue-demi'
Vue.use(VueCompositionApi);

import { Meta, Props, Story, Preview } from '@storybook/addon-docs/blocks';
import Card from './Card.vue';

<Meta title="Card" component={Card} />

# Card

<Preview>
  <Story name="skin: default">
    {{
      components: { Card },
      template: '<Card>There is always light<br>behind the clouds.</Card>',
    }}
  </Story>
  <Story name="skin: background">
    {{
      components: { Card },
      template: '<Card skin="background">There is always light<br>behind the clouds.</Card>',
    }}
  </Story>
  <Story name="skin: background line">
    {{
      components: { Card },
      template: '<Card skin="background-line">There is always light<br>behind the clouds.</Card>',
    }}
  </Story>
</Preview>

<Props of={Card} />

```typescript
import { Card } from 'lapras-frontend'
```

```html
<Card>There is always light<br>behind the clouds.</Card>
```
