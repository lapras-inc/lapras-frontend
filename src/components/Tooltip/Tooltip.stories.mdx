import Vue from 'vue'

import { Meta, Props, Story, Preview } from '@storybook/addon-docs/blocks';
import Tooltip from './Tooltip.vue';

<Meta title="Tooltip" component={Tooltip} />

# Tooltip

<Preview>
  <Story name="default">
    {{
      components: { Tooltip },
      template: `
        <div style="padding: 100px;">
          <Tooltip>これは、長い文章です。これは、長い文章です。これは、長い文章です。これは、長い文章です。これは、長い文章です。これは、長い文章です。
          </Tooltip>
        </div>
      `,
    }}
  </Story>
  <Story name="custom trigger">
    {{
      components: { Tooltip },
      template: `
        <div style="padding: 100px;">
          <Tooltip :options="{placement: 'left-start'}">
            <template #default>hoge</template>
            <template #trigger><img src="//placehold.jp/100x200.png"></template>
          </Tooltip>
          <p style="padding: 30px 0 300px">scroll</p>
        </div>
      `,
    }}
  </Story>
</Preview>

<Props of={Tooltip} />

## options prop

https://popper.js.org/docs/v2/constructors/#options

```typescript
import { Tooltip } from 'lapras-frontend'
```

```html
<Tooltip>tooltip content</Tooltip>
```
