import Vue from 'vue'
import VueCompositionApi from '@vue/composition-api'
Vue.use(VueCompositionApi);

import { Meta, Props, Story, Preview } from '@storybook/addon-docs/blocks';
import CheckBox from './CheckBox.vue';

<Meta title="CheckBox" component={CheckBox} />

# CheckBox

<Preview>
  <Story name="default">
    {{
      data() {return {value: true}},
      components: { CheckBox },
      template: `
        <div>
          <CheckBox v-model="value" />
          <p>{{ value }}</p>
        </div>
      `,
    }}
  </Story>
  <Story name=":checked / @change (boolean)">
    {{
      data() {return {value: true}},
      components: { CheckBox },
      template: `
        <div>
          <CheckBox :checked="value" @change="v => value = v" />
          <p>{{ value }}</p>
        </div>
      `,
    }}
  </Story>
  <Story name=":checked / @change (string)">
    {{
      data() {return {value: 'A'}},
      components: { CheckBox },
      template: `
        <div>
          <label style="display: flex;"><CheckBox value="A" v-model="value" /> A</label>
          <label style="display: flex;"><CheckBox value="B" v-model="value" /> B</label>
          <p>{{ value }}</p>
        </div>
      `,
    }}
  </Story>
  <Story name="v-model(Array)">
    {{
      data() {return {activeMembers: ['B']}},
      components: { CheckBox },
      template: `
        <div>
          <label style="display: flex;"><CheckBox value="A" v-model="activeMembers" /> A</label>
          <label style="display: flex;"><CheckBox value="B" v-model="activeMembers" /> B</label>
          <label style="display: flex;"><CheckBox value="C" v-model="activeMembers" /> C</label>
          <p>{{ activeMembers }}</p>
        </div>
      `,
    }}
  </Story>
  <Story name="disabled">
    {{
      data() {return {value: true}},
      components: { CheckBox },
      template: `
        <div>
          <CheckBox v-model="value" disabled />
          <p>disabled</p>
        </div>
      `,
    }}
  </Story>
</Preview>

```typescript
import { CheckBox } from 'lapras-frontend'
```

```html
<CheckBox v-model="value" />
```

<Props of={CheckBox} />
