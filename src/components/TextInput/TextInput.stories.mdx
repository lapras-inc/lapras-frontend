import Vue from 'vue'

import { Meta, Props, Story, Preview } from '@storybook/addon-docs';
import { action } from "@storybook/addon-actions";
import TextInput from './TextInput.vue';

<Meta
  title="TextInput"
  component={TextInput}
  args={{
    multiline: false,
    error: false,
    disabled: false,
    placeholder: 'placeholder',
    autoExpand: false,
    baseTextareaHeight: 56,
  }}
  argTypes={{
    value: {
      table: { disable: true },
    },
  }}
  />

# TextInput

export const DefaultTemplate = (args, { argTypes }) => ({
  props: Object.keys(argTypes).filter((x) => x !== 'value'),
  components: { TextInput },
  data() {
    return { value: "" }
  },
  methods: {
    onFocus: action('focus'),
  },
  template: `
    <TextInput v-bind="$props" v-model="value" @focus="onFocus"/>
  `,
});

<Preview>
  <Story name="default">
    {DefaultTemplate.bind({})}
  </Story>
</Preview>

<Preview>
  <Story
    name="multiline"
    args={{
      multiline: true,
    }}
  >
    {DefaultTemplate.bind({})}
  </Story>
</Preview>

```typescript
import { TextInput } from 'lapras-frontend'
```

```html
<TextInput v-model="value" />
```

<Props of={TextInput} />
